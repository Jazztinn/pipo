{
  "name": "pipo-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:pipo-menu": "node scripts/sync-pipo-menu.mjs",
    "predev": "npm run sync:pipo-menu",
    "dev": "vite",
    "pretest": "npm run sync:pipo-menu",
    "prebuild": "npm run sync:pipo-menu",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "thanos-end-game": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
